package pl.sda.zad9;

//        7. ** Napisz metodę która przyjmuje zmienną ilość parametrów/argumentów(varargs) LIST i w wyniku zwraca :
//        - jedną listę zawierającą wszystkie elementy (połączenie) wszystkich list
//        - jedną listę zawierającą unikalne elementy ze wszystkich list

public class Main {
//    public <T>List<T> mergeLists(List<T>... lists){
//        return Arrays.stream(lists).flatMap(Collection::stream)
//                .collect(Collectors.toList());
//    }
//
//    public <T>Set<T> mergeSet(List<T>... lists){
//        return Arrays.stream(lists).flatMap(Collection::stream)
//                .collect(Collectors.toSet());
//    }
}
