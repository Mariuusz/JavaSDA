package pl.sda.zad8;

//        6. Napisz metodę, która przyjmuje dowolną liczbę liczb typu double (varargs) i tworzy zbiór (HashSet), składający się z tych elementów, lub wyrzuca IllegalArgumentException, jeżeli elementy nie są unikalne
//public static Set<Double> newSet(double numbers…)
//
//        -**spróbuj przerobić metodę na wersję generyczną
//public static <T> Set<T> newSet(T... data)

public class Main {

//    public <T>Set<T> newSet(T... numbers){
//        Set<T> uniqueNumbers = new HashSet<>();
//        for(T d: numbers){
//            if(!uniqueNumbers.add(d)) throw new IllegalArgumentException();
//        }
//        return uniqueNumbers;
//    }
}
